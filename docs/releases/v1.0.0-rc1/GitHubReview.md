# GitHub Review

GitHub CLI review inspection was not available in this container because `gh` is not installed.

When running in an environment with GitHub CLI access, record here:

- open PR status
- unresolved review threads
- CI or status-check blockers
- release-blocking labels or requested changes
