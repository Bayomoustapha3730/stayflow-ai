# Modules

Suggested module split for future refinement:

- observability
- registry and key vault
- database
- container apps
- alerts

The initial deployment template is defined in `../main.bicep`.
